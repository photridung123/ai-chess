import { StyleSheet } from "react-native";

const StyleStart = StyleSheet.create({
    container: {
        flex: 1,
    },

    boxInput: {
        borderColor: "#E7E1B5",
        borderRadius: 50,
        marginLeft: 20,
        marginRight: 20,
        paddingTop: 30,
        paddingBottom: 30,
    },

    background: {
        flex: 1,
        resizeMode: 'cover',
    },

});
export default StyleStart;